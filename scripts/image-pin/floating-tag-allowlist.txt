# 浮动 tag 豁免清单 —— verify-no-floating-tags.sh 读取
#
# ★这是**技术债登记簿**，不是许可证。每一条都是一个「安全修复可能静默缺席」的
#   敞口：镜像内容变了而引用不变，集群拿到什么完全取决于上游何时推送。
#   aster-lsp 就是这么让 token 门缺席七个月的（见脚本头部）。
#
# ★当前清单为空 —— 全部存量浮动 tag 已收敛为 digest pin（k3s#523，已实测 9/0）。
#   请尽量保持为空：新增一条就是新增一个上述敞口。
#
# 登记格式：一行一个**渲染后的完整镜像引用**（与守卫的比较口径逐字一致）。
# 每条上方必须写明：为什么暂时豁免、谁负责收敛、复审日期。
#
# 例（历史条目，已收敛，仅作格式示范）：
#   # cloudflare-tunnel：第三方镜像，需人工跟随上游升级。复审：2026-12-01
#   cloudflare/cloudflared:2024.12.2
