# Kustomization for External Secrets
# Only includes Application manifests - ClusterSecretStore is managed by config-application
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - application.yaml
  - config-application.yaml
