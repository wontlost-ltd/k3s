# Aster API Deployment
# Aster API - Aster Lang CNL Evaluation Engine
apiVersion: apps/v1
kind: Deployment
metadata:
  name: aster-api
  namespace: aster-cloud
  labels:
    app: aster-api
    app.kubernetes.io/name: aster-api
    app.kubernetes.io/part-of: aster-lang
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aster-api
  template:
    metadata:
      labels:
        app: aster-api
        app.kubernetes.io/name: aster-api
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: aster-api
          image: docker.io/wontlost/aster-api:jvm-latest  # managed by ArgoCD + imagePullPolicy:Always ensures freshness
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          env:
            # OTel tracing → otel-collector → tempo（SNAP-10）
            # Disabled: current wontlost/aster-api:jvm-latest doesn't include
            # opentelemetry-exporter-otlp on classpath. Re-enable once the
            # quarkus-opentelemetry-exporter-otlp extension is added to the
            # image build. See aster-api crash:
            #   io.opentelemetry.sdk.autoconfigure.spi.ConfigurationException:
            #   otel.traces.exporter set to "otlp" but
            #   opentelemetry-exporter-otlp not found on classpath
            - name: OTEL_TRACES_EXPORTER
              value: "none"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://otel-collector.otel-system.svc:4317"
            - name: OTEL_EXPORTER_OTLP_PROTOCOL
              value: "grpc"
            - name: OTEL_TRACES_SAMPLER
              value: "parentbased_traceidratio"
            - name: OTEL_TRACES_SAMPLER_ARG
              value: "0.1"  # 10% 采样
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: "service.name=aster-policy-api,service.version=jvm-latest,deployment.environment=prod"
            # 数据库连接（从 ExternalSecret 注入）
            - name: QUARKUS_DATASOURCE_JDBC_URL
              valueFrom:
                secretKeyRef:
                  name: aster-api-db-credentials
                  key: jdbc-url
            - name: QUARKUS_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: aster-api-db-credentials
                  key: username
            - name: QUARKUS_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: aster-api-db-credentials
                  key: password
            # Reactive datasource URL (for Flyway/Hibernate)
            - name: QUARKUS_DATASOURCE_REACTIVE_URL
              valueFrom:
                secretKeyRef:
                  name: aster-api-db-credentials
                  key: reactive-url
            # Redis Standalone 连接配置
            - name: QUARKUS_REDIS_HOSTS
              valueFrom:
                secretKeyRef:
                  name: aster-api-redis-credentials
                  key: redis-hosts
            - name: QUARKUS_REDIS_CLIENT_TYPE
              value: "standalone"
            # CORS 配置
            - name: QUARKUS_HTTP_CORS_ORIGINS
              value: "https://aster-lang.cloud,https://www.aster-lang.cloud,https://policy.aster-lang.dev,https://aster-lang.dev"
            # LLM AI Assistant（从 Vault 注入）
            - name: ASTER_LLM_API_KEY
              valueFrom:
                secretKeyRef:
                  name: aster-api-llm-credentials
                  key: api-key
            - name: ASTER_LLM_BASE_URL
              valueFrom:
                secretKeyRef:
                  name: aster-api-llm-credentials
                  key: base-url
            - name: ASTER_LLM_PROVIDER
              valueFrom:
                secretKeyRef:
                  name: aster-api-llm-credentials
                  key: provider
            - name: ASTER_LLM_MODEL
              valueFrom:
                secretKeyRef:
                  name: aster-api-llm-credentials
                  key: model
            # 安全配置：禁用 HMAC 签名验证（内部网络访问）
            - name: ASTER_SECURITY_SIGNATURE_ENABLED
              value: "false"
            # NSM 埋点 — Mixpanel HTTP 投递（详见 aster-deploy/docs/pm/03-telemetry-spec.md）
            # Optional：Vault path secret/apps/aster-api-mixpanel 未配置时 pod 仍可启动，
            # 但 telemetry 不会生效（ASTER_MIXPANEL_TOKEN 为空时客户端自动禁用）。
            - name: ASTER_MIXPANEL_ENABLED
              value: "true"
            - name: ASTER_MIXPANEL_TOKEN
              valueFrom:
                secretKeyRef:
                  name: aster-api-mixpanel-credentials
                  key: token
                  optional: true
            # 跨服务 PlanGate — aster-api → aster-cloud /api/internal/tenant/{id}/plan
            # 详见 aster-deploy/docs/pm/06-cross-service-plan-gate.md
            # Optional：Vault path secret/apps/aster-api-plan-gate 未配置时 fail-open，
            # plan 升级生效会延迟到 cache TTL 兜底（5min）。
            - name: ASTER_PLAN_GATE_ENABLED
              value: "true"
            - name: ASTER_CLOUD_INTERNAL_URL
              value: "http://aster-cloud.aster-cloud.svc.cluster.local:3000"
            - name: ASTER_PLAN_GATE_HMAC_KEY
              valueFrom:
                secretKeyRef:
                  name: aster-api-plan-gate-credentials
                  key: hmac-key
                  optional: true
            # Staging 工具端点 — 生产显式禁用（防御深度）
            - name: ASTER_STAGING_TELEMETRY_ENABLED
              value: "false"
            # JVM 调优（缩减堆内存）
            - name: JAVA_OPTS
              value: "-Xms128m -Xmx384m -XX:+UseG1GC -XX:MaxGCPauseMillis=100"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          resources:
            requests:
              memory: 256Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 500m
          livenessProbe:
            httpGet:
              path: /q/health/live
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /q/health/ready
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /q/health/started
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 30
      volumes:
        - name: tmp
          emptyDir: {}
