apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: aster-slo-burn-rate
  namespace: aster-observability
  labels:
    app.kubernetes.io/part-of: aster-lang
    app.kubernetes.io/component: slo
    release: prometheus
spec:
  # SLO burn-rate placeholder — fill in actual SLO rules when Tempo / metrics
  # data is wired up (Phase 3E observability work). Empty groups makes the
  # CRD valid without firing any alerts.
  groups: []
