# Aster API Application
# Sample application for aster-lang.dev domain
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: aster-cloud

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - ingress-deny-evaluate-source.yaml
  - pdb.yaml
  # TODO: Re-enable when k3s kube-router cross-node network policy issue is resolved
  # See: https://github.com/k3s-io/k3s/issues - kube-router has bugs with NetworkPolicy
  # - network-policy.yaml
  - external-secrets.yaml
  # aster-cloud drizzle migration (PreSync hook + standalone runs)
  # 临时禁用：等待 aster-cloud 仓库配齐 DOCKERHUB_* secret + CI 构建出
  # wontlost/aster-cloud-migrate:latest 镜像后再启用。
  # - migrate-external-secret.yaml
  # - migrate-job.yaml
